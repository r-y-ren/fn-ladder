# FN-LADDER 函数阶梯总览

以函数为颗粒度的五阶段开发流。存在理由：AI 编码的**假完成**——自称做完而实际漏实现、或函数存在但没接线。对策四件套：**函数级对齐、四态判据、双层真值、阶段硬门**。

## 流程

```
fn-refactor（存量项目 on-ramp）─┐
                                 ├──> fn-grill ──门──> fn-divide ──门──> fn-scaffold ──门──> fn-implement ──门──> fn-close
fn-merge（多项目合并 on-ramp）──┘      需求打磨        函数划分         结构+骨架          分批自底向上实现    收尾核验
```

两个 on-ramp 汇入正常流程：fn-refactor（两轮 grill——用户先讲流程功能、代码审查找差距、行为清单逐条裁决、快照测试兜底——之后以一次完整 fn-ladder 任务把存量代码重构进标准结构，验收后删旧码）；fn-merge（以代码现实现为准产已实施清单 → 只依源需求文档合并 README/requirements → 汇总 responsibility.md，之后进入正常流程；函数级修改/重命名只记录在新 implementation.md 里）。

横向能力随时可用、不占阶段位：**fn-review**（只读审计：机械轴+规格轴）· **fn-brainstorm**（卡壳发散：≥3 方案含激进项）· **fn-analyze**（数据驱动分析：拉运行结果→三轴分析→函数级提案→效果闭环打分）。

## 四条不变式（全流程生效）

1. **代码是真值，文档是导航。** 改任何状态标注前必须先跑核验命令、贴出输出，绿了才许改。
2. **阶段硬门。** 每阶段结束停下，**显式提示两项：下一步命令（如 `/fn-divide`）或修订本步骤**；未经用户显式选择，禁止推进到下一阶段。禁止以"任务很简单/用户大概希望我继续"为由越门。on-ramp（fn-refactor/fn-merge）只完成自身阶段，出口交接给正常流程的单个技能显式调用，不得一技能跑完全程。
3. **文档所有权分离。** 每份文档只有一个写入阶段，其他阶段只读：

   | 产物 | 写入者 | 内容 |
   |---|---|---|
   | `fn_docs/README.md` + `requirements.md` | fn-grill | README=用户向通俗说明（静态）；requirements=需求、术语、验收方式、范围外、变更记录 |
   | `fn_docs/responsibility.md` | fn-divide | 需求覆盖矩阵、分层函数树（职责/签名意图/调用方/核验命令） |
   | `fn_docs/implementation/` 三文档 | fn-implement | batches.md 批次表（待办导航，▶ 下一批）+ functions.md 函数大表（唯一状态真值：四态+证据+commit）+ history.md 历史表（批次留痕，只追加） |
   | `fn_docs/inventory.md` | fn-merge | 已实施清单（代码现状快照，两步生成：先代码普查后对照责任文档；进入正常流程后只读） |
   | `fn_docs/results/` + `fn_docs/analyses/<序号>-<日期>.md` | fn-analyze | 运行结果数据快照与分析报告（含历史提案预期信号打分——效果闭环）；数据源定义在 requirements 外部依赖节 |
   | `fn_docs/acceptance.md` | fn-close | 验收报告：五道终检（+merge 源函数去向表）的事实一览与原始输出；终审归用户 |
   | 代码 | fn-scaffold（结构）/ fn-implement（实现） | 最终真值 |

   越界写入即违规：fn-implement 发现结构性变化（函数增/删/拆/并/职责或调用关系变化）必须停下，回 fn-divide 改 responsibility.md 后再回来；需求变更回 fn-grill 记变更、fn-divide 重算受影响子树。小变更可走快速通道（一次确认式编辑），但必须由 owning 阶段的规则执行、写入 owning 阶段的文档。`fn_docs/` 下所有文档默认随代码提交进版本库。生命周期：**验收后原地保留**；`fn_docs/` 存在即表示任务已有进度，后续按中途进入协议续走，不做强制清理。**任务放弃**：用户显式宣布，当刻阶段在最新流程文档的变更记录写放弃行（日期/原因/停在哪步），代码处置与 `fn_docs/` 去留由用户定。**单任务约束**：一个工作目录同时只有一个 fn-ladder 任务；并行就分工作目录或分支。**小任务豁免**：≤2 函数或纯配置/样式/文档类工作，经确认走常规流程，不进本流程。

4. **中途进入靠对账，不靠对话记忆。** 进入任一阶段先做：① 检查前置产物是否存在（缺→停，指回前序阶段）；② 轻量对账——重跑全局核验（grep 桩标记、跑全测试、逐函数查调用点）；③ 与文档对账，报告真实进度；④ 从实际位置继续。

## 四态判据（fn-implement 内逐函数推进）

`stub`（签名+统一标记桩）→ `implemented`（桩被真实实现替换）→ `tested`（叶子函数必须自有单测绿；中上层可标"上游覆盖"并指向具体测试）→ `wired`（静态调用点存在；顶层入口函数必须实跑一次、有可观察输出）。

## 防假完成双检查（fn-divide 建立矩阵，fn-close 终检）

需求条目无函数负责 = **漏实现**；函数无调用链连到顶层入口 = **死代码**。两种假完成，一进一出双向拦截。

## 功能演进周期（v1.2）

验收不是终点：同一项目追加功能 = **再走一轮五阶段（功能演进模式）**，同一套 `fn_docs/` 演化——

- **识别**（fn-grill 进入检查三分支）：无 `fn_docs/` = 新任务；有 `fn_docs/` 无 `acceptance.md` = 续作；有 `acceptance.md` = 演进模式（默认）。
- **演进链**（每步产物呈现、过门口确认后推进）：
  1. **fn-grill**：新策略谈定并经用户同意后，**必然落盘**——README 追加功能段、requirements 追加新 R 条目（编号续号）；
  2. **fn-divide**：responsibility.md **只增不改旧块**——矩阵加行、函数树加新功能块（改造现有函数标 `[改造]`）；
  3. **fn-scaffold（仅当有新函数）**：只为新函数加桩、不动已 wired 代码；纯改造现有函数则**跳过本步**；
  4. **fn-implement**：更新 implementation/ 三文档开始实施——batches.md 追加新批次（B 续号）、functions.md 追加新函数行（改造函数重走四态）；
  5. **fn-close**：全量终检（新旧一起过五道），旧报告归档 `acceptance-c<N>.md`。
- **补充规则**（链外要点）：历史表保留旧周期记录；终检全量含旧函数与旧需求；已 wired 旧函数不动（结构性调整经快速通道）。
- **配套**：演进开工前强烈建议先跑 `fn-review` 审计上周期之后的手动改动与文档漂移；策略卡壳用 `fn-brainstorm` 发散；有运行结果数据（对战/评测/基准）的改进用 `fn-analyze`：拉数据 → 三轴分析（结果/代码机械/职责关联）→ 函数级提案（五字段，含预期信号）→ 效果闭环打分 → 交 grill 开 R。
- 单任务约束不变：同一时间只有一个活跃周期。

## 检查脚本与子代理（v1.1）

- **脚本**（插件根 `scripts/`，技能一律"跑脚本、贴输出"，铁律不变）：
  - `fn-check.sh`——代码侧三件套：残留桩 grep、全量测试（FN_TEST_CMD 或自动探测）、tests 镜像抽查。
  - `fn-doc-lint.py`——文档侧机械校验：requirements 八节与 R 编号、functions 状态词与函数覆盖、batches ▶ 唯一与批次/历史不重叠、responsibility 树块一致、矩阵双向、函数名唯一、调用链可达到入口（死代码拦截）。
- **子代理**：①**对账子代理**——fn-implement 进入时派，跑 fn-check 并对照 functions.md，主会话只收结论（隔离对账输出）；②**评审子代理**——批间门前派，双轴审查批 diff（规格轴 vs 责任文档 / 标准轴：桩标记、命名、目录规则），用户可免审；③**逐函数实现子代理**——可选档，单批 ≥6 函数建议，每函数带紧凑提示（职责块+桩位置+核验命令），主会话只做调度与门。
- **钩子**：v1 不配——强制力的终点是 workflow 固化，不在半山腰重复建设；真实任务暴露"模型跳步"案例后再议。

## 固化为 workflow 的接口

阶段边界 = workflow 的 phase 边界；阶段门 = 脚本强制 gate；轻量对账 = 落盘产物重放（journal 恢复）。流程经真实任务打磨稳定后，按此映射固化。
