---
name: fn-close
description: 当实现清单全员 wired（或用户要求提前验收）、需要终检并产出验收报告时使用——fn-ladder 流程第五阶段（收尾核验）。用户提到 fn-close、验收、终检、这个任务做完了吗时也用。
---

# fn-close：收尾核验（fn-ladder 第⑤阶段）

对整个任务做终检并产出验收报告。本阶段只读全部文档与代码、只写验收报告，**不改任何前序产物**。

## 进入检查

`implementation/functions.md` 函数大表全员 wired（用户明确要求提前验收时也可进入，报告中注明未完成项）。中途接手先按 FN-LADDER.md 中途进入协议对账。

## 五道终检（全过才算完成）

1. **无残留桩**：全库 grep `unimplemented:fn:` 零命中。
2. **全量测试绿**：跑全量测试，贴原始输出。
3. **接线实据**：逐函数静态调用点存在；顶层入口函数有实跑记录与可观察输出。
4. **矩阵双向复查**（对照 responsibility.md 需求覆盖矩阵）：每条需求有函数负责且已 wired——**漏实现拦截**；每个函数有调用链连到顶层入口——**死代码拦截**。
5. **非功能约束对照**（对照 requirements.md 非功能节）：逐条验证约束与判据。

（merge 任务另有第六道：**源函数去向表**——inventory.md 每个函数必须有下落：保留 / 重命名→新名 / 合并→目标 / 删除→**理由必填**；无下落或删除无理由 = 终检不过。）

## 验收报告（fn_docs/acceptance.md，本阶段独占写入；同时在对话中呈现）

```markdown
# 验收报告：<任务名>
> fn-close 独占产出。核验输出原样粘贴，禁止转述、禁止"概括为通过"。
> 本报告不宣布"验收通过"——终审归用户。

## 终检一览
| 终检 | 命令 | 结果 | 证据 |
|---|---|---|---|
| ① 无残留桩 | grep -rn "unimplemented:fn:" | 0 命中 | §1 |
| ② 全量测试 | <全量测试命令> | 全绿 | §2 |
| ③ 接线与实跑 | — | 过 | §3 |
| ④ 矩阵双向 | — | 过 | §4 |
| ⑤ 非功能对照 | — | 逐条过 | §5 |

## 1 桩检查（全量输出原文）
## 2 全量测试（全量输出原文）
## 3 接线与实跑（逐顶层函数：调用点检查 + 实跑输出原文）
## 4 需求→函数对照表（R → 函数 → 核验命令 → 结果行）
## 5 非功能约束逐条（约束 → 判据 → 验证输出）
## 未完成项（仅提前验收时：函数 | 当前状态 | 原因）
## 源函数去向表（仅 merge 任务：源函数 | 去向 | 说明/理由）
```

- **粘贴分级**：全局性命令（①②③的实跑）贴全量原文；函数级证据在 §4 记命令+结果行——全量测试输出已天然覆盖每个函数的测试结果。原样粘贴铁律约束的是**贴进来的部分不许转述**。
- 一览表只报事实（零命中 / 全绿 / N 项过），**任何位置不出现"验收通过 ✓"式自封结论**；报告末行固定一句：**"以上为终检事实，验收与否由用户判定。"**
- 任何一道终检不过：如实报告未过项与缺口，指回对应阶段（实现→fn-implement、划分→fn-divide、需求理解→fn-grill），**不许宣布完成**；修复后重跑**全部**终检、**重写** acceptance.md（旧版作废，git 历史留痕）——验收是全量快照，不是流水账。

## 收尾

- 验收后 `fn_docs/`（含 acceptance.md）**原地保留**供查阅、随 git 留痕；不移动、不归档、不清理。
- `fn_docs/` 此后存在即表示任务已有进度：后续会话按中途进入协议续走。
- **终审出口**：报告呈现后验收与否由用户判定；任务闭环时显式提示后续入口——新任务 → 新工作目录（或处置 fn_docs 后）`/fn-grill`；存量项目 → `/fn-refactor`；多项目合并 → `/fn-merge`。
- **终审停**：报告呈给用户，由用户判定验收——完成与否是用户的结论，不是本阶段的自我声明。

## 红旗（出现即停）

| 念头 | 现实 |
|---|---|
| "都 wired 了，肯定没问题" | 五道终检是独立防线，wired 是导航层不是真值 |
| "测试输出太长，概括一下" | 转述的输出 = 无法审计的证据 |
| "这个小残留下个任务再说" | 残留桩 = 未完成，如实报告 |
